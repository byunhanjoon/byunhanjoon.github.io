# Metric-Field Transport

Post-outcome development ladder for the successor to the failed frozen MPE
tokenizer. Read `DEVELOPMENT_PROTOCOL.md` before running anything.

The original `experiments/mpe_iclr` test partition remains sealed. Results in
this directory are development evidence only.

The write-once runners are in `successor_experiments.py`. Regenerate the
completeness audit, promotion gate, and human-readable report with
`analyze_successor.py`.
