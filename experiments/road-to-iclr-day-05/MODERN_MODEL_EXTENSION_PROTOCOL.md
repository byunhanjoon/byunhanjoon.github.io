# Post-source modern-model extension

Frozen before fitting either added model family, but after observing both late
source blocks. This is a model-family extension, not a new-source confirmation.

## Panel

Reuse the eight untouched OpenML source identities, exact splits, nuisance
menus, seeds, and complete `4 x 4 x 2 x 4 = 128` products from source blocks A
and B. Add two already-implemented, stronger tabular baselines absent from
those blocks:

- native categorical-aware histogram gradient boosting;
- CatBoost with native categorical fields.

This produces 16 new tensors and 2,048 new fits. No source, model, or failed
cell may be replaced. The categorical-only adapter already disclosed for the
source experiments remains in force.

## Frozen descriptive gates

For strength-2 versus IID-16 and four strength-1 blocks, require at least 13/16
candidate wins and 7/8 equal-source-mean wins. For pair32, pack64, and unbiased
pair-cross64 versus their equal-fit controls, require the same 13/16 candidate
and 7/8 source thresholds. Report all failures regardless of materiality.

These thresholds only test model-family transport on previously seen sources.
They cannot establish prospective source generalization or predictive SOTA.
