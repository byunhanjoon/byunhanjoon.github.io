# Introduction Notes

Motivation: general tabular predictors approximate arithmetic relations that a
deterministic executor can compute exactly. The first required evidence is a
controlled magnitude-extrapolation result, not a broad benchmark win.
